<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.ol_account_hot_ar</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="50"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <setting id="total_amount_words" position="before">
                <setting string="Hot AR Grace Period" company_dependent="1" help="Places holds on customer orders when invoices are not paid within grace period of being due.">
                    <field name="hot_ar_grace_period" />
                </setting>
            </setting>
        </field>
    </record>

</odoo>
