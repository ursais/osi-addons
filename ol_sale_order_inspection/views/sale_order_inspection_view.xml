<odoo>

    <record id="view_sale_order_inspection_tree" model="ir.ui.view">
        <field name="name">sale.order.inspection.tree</field>
        <field name="model">sale.order.inspection</field>
        <field name="arch" type="xml">
            <tree editable="top">
                <field name="name" />
                <field name="group_ids" widget="many2many_tags" />
                <field name="color" widget="color_picker" />
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_sale_order_inspection">
        <field name="name">Order Inspections</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">sale.order.inspection</field>
        <field name="view_mode">tree</field>
    </record>

    <menuitem
        id="menu_sale_order_inspection"
        name="Order Inspections"
        parent="sale.menu_sale_config"
        sequence="1000"
        action="action_sale_order_inspection"
    />

</odoo>
