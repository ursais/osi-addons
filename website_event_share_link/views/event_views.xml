<odoo>

    <record id="model_event_event_action_share" model="ir.actions.server">
        <field name="name">Share</field>
        <field name="model_id" ref="website_event_share_link.model_event_event"/>
        <field name="binding_model_id" ref="website_event_share_link.model_event_event"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">action = records.action_share()</field>
    </record>
</odoo>
