<odoo>
    <record id="res_config_settings_view_form_inherit_ol_bo" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.bo</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale_blanket_order.sale_config_settings_form_view"/>
        <field name="arch" type="xml">
            <block name="blanket_orders_setting_container" position="after">
            	<setting
                        id="blanket_order_release_days"
                        title="Number of days before scheduled date to release blanket orders"
                        help="Number of days before scheduled date to release blanket orders"
                    >
                        <field name="blanket_order_release_days" />
                    </setting>
            </block>
        </field>
    </record>
</odoo>
