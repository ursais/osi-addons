<odoo>

    <record id="action_create_update_scaffold_bom" model="ir.actions.server">
        <field name="name">Create/Update Scaffold BoM</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="product.model_product_template" />
        <field name="binding_model_id" ref="product.model_product_template" />
        <field name="state">code</field>
        <field name="code">
for rec in records:
    rec.action_create_rebuild_scaffolding_bom()
        </field>
    </record>

</odoo>
