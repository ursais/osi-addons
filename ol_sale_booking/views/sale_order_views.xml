<odoo>

    <!-- Form View -->
    <record id="ol_sale_booking_view_order_form" model="ir.ui.view">
        <field name="name">ol_sale_booking.view_order_form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="priority">30</field>
        <field name="arch" type="xml">
            <xpath expr="/form[1]/sheet[1]/notebook[1]/page[last()]" position="after">
                <page string="Bookings">
                    <field name="booking_ids" nolabel="1" readonly="1">
                        <tree string="Bookings" default_order="id desc">
                            <field name="id" optional="show" />
                            <field name="create_date" optional="show" />
                            <field name="currency_id" column_invisible="1" />
                            <field name="amount" sum="Booking Amount" optional="show" />
                            <field name="amount_untaxed" sum="Booking Amount Untaxed" optional="show" />
                            <field name="order_amount_total" sum="Total Amount" optional="show" />
                            <field name="order_amount_total_untaxed" sum="Untaxed Amount" optional="show" />
                            <field name="tax_amount" sum="Tax Amount" optional="show" />
                            <field name="order_prev_amount_total" sum="Previous Total Amount" optional="hide" />
                            <field name="order_prev_amount_total_untaxed" sum="Previous Total Amount Untaxed" optional="hide" />
                            <field name="prev_tax_amount" sum="Previous Tax Amount" optional="hide" />
                            <field name="order_detailed_state" optional="hide" />
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <record id="action_sale_booking_tree" model="ir.actions.act_window">
        <field name="name">Sale Bookings</field>
        <field name="res_model">sale.booking</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="sale_booking_tree" />
        <field name="search_view_id" ref="sale_booking_tree_search" />
        <field name="context">{'search_default_last_6_months': True}</field>
    </record>

    <record id="action_sale_booking_line_tree" model="ir.actions.act_window">
        <field name="name">Sale Booking Lines</field>
        <field name="res_model">sale.booking.line</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="sale_booking_line_tree" />
        <field name="search_view_id" ref="sale_booking_line_tree_search" />
        <field name="context">{'search_default_last_6_months': True}</field>
    </record>

    <menuitem
        name="Sale Bookings"
        id="menu_sale_booking_main"
        parent="sale.sale_menu_root"
    />

    <menuitem
        id="menu_sale_booking"
        action="action_sale_booking_tree"
        parent="ol_sale_booking.menu_sale_booking_main"
        sequence="10"
    />

    <menuitem
        id="menu_sale_booking_lines"
        action="action_sale_booking_line_tree"
        parent="ol_sale_booking.menu_sale_booking_main"
        sequence="20"
    />
</odoo>
