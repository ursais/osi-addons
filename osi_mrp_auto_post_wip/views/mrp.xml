<odoo>
   <record id="action_action_post_inventory_wip" model="ir.actions.server">
        <field name="name">Post WIP</field>
        <field name="model_id" ref="mrp.model_mrp_production" />
        <field name="binding_model_id" ref="mrp.model_mrp_production" />
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field
            name="code"
        >records.filtered(lambda m: m.state not in ('draft', 'confirmed', 'done', 'cancel')).action_post_inventory_wip()</field>
    </record>
</odoo>
